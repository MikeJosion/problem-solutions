# [23. 合并 K 个升序链表](https://leetcode.cn/problems/merge-k-sorted-lists/)**(12.18)**

> 💡 **给你一个链表数组，每个链表都已经按升序排列。请你将所有链表合并到一个升序链表中，返回合并后的链表。(21. 合并两个有序链表的进阶版本)**
>
**示例 1：**

```plain text
输入：lists = [[1,4,5],[1,3,4],[2,6]]
输出：[1,1,2,3,4,4,5,6]
解释：链表数组如下：
[
  1->4->5,
  1->3->4,
  2->6
]
将它们合并到一个有序链表中得到。
1->1->2->3->4->4->5->6
```

## 难点

<details><summary>1️⃣`struct ListNode** lists`是什么意思?</summary>

> 💡 **`struct ListNode** lists`表示「一个存放多条链表头指针的数组」；**
>
> 其中`lists[i]` 就是第 i 条链表的头结点指针。
> > 
> ## 一、从最小单位拆开看
> > 
> ### 1️⃣ `struct ListNode`
> > 表示**一个链表结点**：
> > 
> ```c
> structListNode {
> int val;
> structListNode*next;
> };
> ```
> > 
> ---
> > 
> ### 2️⃣ `struct ListNode*`
> > 表示：
> > > 指向一个链表结点的指针
> > 在链表题里，通常就是：
> > > 一条链表的头指针
> > 
> ```c
> structListNode*head;// 一条链表
> ```
> > 
> ---
> > 
> ### 3️⃣ `struct ListNode**`
> > 再加一个 `*`，表示：
> > > 指向“链表头指针”的指针
> > 在这道题里，它的真实用途是：
> > > 表示“链表头指针数组”
> > 
> ---
> > 
> ## 二、在 LeetCode 23 中的真实含义（重点）
> > 函数签名：
> > 
> ```c
> struct ListNode*mergeKLists(struct ListNode** lists,int listsSize);
> ```
> > 这里的：
> > 
> ```c
> structListNode**lists
> ```
> > 可以等价理解为：
> > 
> ```c
> structListNode*lists[];
> ```
> > 也就是：
> > > 一个数组，数组的每个元素，都是一条链表的头指针
> 
</details>

<details><summary>2️⃣返回的两条列表中,由于不确定返回的是前还是后的链表,在循环套用函数时我怎么写入?</summary>

> 💡 ****
>
> 
> ### 一、核心问题
> > 在循环调用 `mergeTwoLists` 时，
> > **返回的链表头结点可能来自前一条链表，也可能来自后一条链表，具有不确定性。**
> > 
> ---
> > 
> ### 二、正确理解（最关键）
> > > mergeTwoLists 返回的是“合并后新链表的头结点”，
> 至于该头结点来自哪一条链表，属于函数内部实现细节，调用者无需关心。
> > 
> ---
> > 
> ### 三、正确的写入方式（标准模板）
> > 
> ```c
> struct ListNode*res =NULL;
> 
> for (int i =0; i < listsSize; i++) {
>     res = mergeTwoLists(res, lists[i]);
> }
> 
> return res;
> ```
> > 
> ---
> > 
> ### 四、为什么这样写是对的
> > - `res` 始终保存**当前已合并好的链表**
> > - 每次循环：
>   - 将“当前结果”和“下一条链表”合并
>     - 用返回值**覆盖更新 **`res`
>   > - 不需要判断返回的是前链表还是后链表
> 
</details>

<details><summary>3️⃣ 循环条件的判定和数组下标越界（`i + 1`）</summary>

> 💡 ****
>
> 
> ```c
> ❌ 错误写法：
> 
> for(int i = 0;i<listsSize;i++){
> 	 mergeTwoLists(list[i],list[i+1]);
>   }
> 
> ❌ 错误：
> 当 i == listsSize - 1 时：lists[i+1]  // 越界访问 ❌
> 合并了，但没接住返回值 mergeTwoLists(lists[i], lists[i+1]); ⚠️ mergeTwoLists 返回的是新链表的头结点，不接收，结果直接丢了。
> 
> 
> ✅ 正确理解：
> **核心认知**
> • `lists` 是一个 **长度为 **`listsSize`** 的数组**
> • 合法下标是：`0 ~ listsSize - 1`
> 当：`i == listsSize - 1`
> 你访问的是：`lists[i + 1] == lists[listsSize]   // ❌ 不存在
> `**
> 正确理解总结凡是写 **`i + 1`**，循环条件就不能写 **`i < listsSize`**
> mergeTwoLists 是一个“纯函数”
> 输入：两个链表头指针
> 输出：一个新的链表头指针
> 不会自动替你保存结果
> 📌 合并的“状态”必须由你自己维护**
> 
> 😱认识错误点后的写法:
> struct ListNode* mergeKLists(struct ListNode** lists, int listsSize) {
>     struct ListNode* temp = lists[0];
>     for(int i = 1;i<listsSize;i++){
>         temp = mergeTwoLists(temp,lists[i]);
>     }
>     return temp;
> }
> 必出问题 1️⃣：listsSize == 0 时直接崩溃 ❌发生越界访问导致程序崩溃
> 
> ✅ 正确判断：
> 
> struct ListNode* mergeKLists(struct ListNode** lists, int listsSize) {
>     if (listsSize == 0){
>         return NULL;
>     }
>     struct ListNode* temp = lists[0];
>     for(int i = 1;i<listsSize;i++){
>         temp = mergeTwoLists(temp,lists[i]);
>     }
>     return temp;
> }
> ```
> 
</details>


## 🏗️ 核心知识
> 💡 ****
>
> 
> ## 一、二级指针在函数参数中的实际用途
> > 
> ### 知识点
> > 
> ```c
> structListNode**lists
> ```
> > - **二级指针不一定是为了修改指针**
> > - 在这里它的作用是：**表示“指针数组”**
> > - 即：**多条链表的头指针集合**
> > 
> ### 关键理解
> > 
> ```c
> lists[i]// 类型是 struct ListNode*
> ```
> > 
> ---
> > 
> ## 二、C 语言中“数组化为指针”的规则
> > 
> ### 知识点
> > 
> ```c
> structListNode**lists
> ≡
> structListNode*lists[]
> ```
> > - 数组作为函数参数时，会自动退化为指针
> > - 所以 LeetCode 用 `*` 表示链表数组
> > 
> ---
> > 
> ## 三、函数返回值必须显式接收（链表场景）
> > 
> ### 知识点
> > 
> ```c
> res = mergeTwoLists(res, lists[i]);
> ```
> > - **函数不会自动修改调用者的变量**
> > - 合并结果必须通过返回值接收
> > - 链表合并 ≠ 原地自动生效
> > 
> ---
> > 
> ## 四、合并结果“头结点来源不确定”的正确处理方式
> > 
> ### 知识点
> > - 合并后的头结点：
>   - 可能来自前一条链表
>     - 也可能来自后一条链表
>   > 
> ### 正确认知
> > > 调用者不关心来源，只关心返回值
> > 
> ---
> > 
> ## 五、用“滚动变量”维护合并状态
> > 
> ### 知识点
> > 
> ```c
> structListNode*cur =NULL;
> ```
> > - 用一个指针变量保存“当前合并结果”
> > - 每轮循环覆盖更新
> > - 避免多指针混乱
> 
## 💻代码

```c
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */


struct ListNode* mergeTwoLists(struct ListNode* l1,struct ListNode* l2){
    if(l1 == NULL || l2 == NULL){
        return l1 != NULL ? l1 : l2;
    }else if(l1->val > l2->val){
        l2->next = mergeTwoLists(l2->next,l1);
        return l2;
    }else {
        l1 -> next = mergeTwoLists(l1->next,l2);
        return l1;
    }
}

struct ListNode* mergeKLists(struct ListNode** lists, int listsSize) {
    if (listsSize == 0){
        return NULL;
    }
    struct ListNode* temp = lists[0];
    for(int i = 1;i<listsSize;i++){
        temp = mergeTwoLists(temp,lists[i]);
    }
    return temp;
}
```
